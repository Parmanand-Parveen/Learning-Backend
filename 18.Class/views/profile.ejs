<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>

<body>
    <div class="bg-zinc-900 min-h-screen w-screen text-white">
        <div class="p-5">
            <div class="flex w-full justify-between">
                <p><span class="text-2xl">Hello ,</span>
                    <%= user.username%>👋🏻
                </p>
                <a href="/logout"
                    class="bg-red-500 hover:bg-red-700 transition-all rounded-md flex items-center px-3 py-2 ">Logout</a>
            </div>

            <form class="mt-3" action="/post" method="post">
                <textarea class="resize-none rounded-md p-2 mb-2 bg-zinc-800 border block border-zinc-600 outline-none"
                    placeholder="What's in your mind" cols="50" rows="3" name="content" id=""></textarea>
                <button type="submit"
                    class="px-3 py-2 bg-blue-500 border-none block rounded-md text-white hover:bg-blue-700 transition-all"
                    >Create post</button>
            </form>

          <% user.posts.reverse().map(function(post){   %>
            <div class="w-1/3 bg-zinc-800 px-3 py-2 border border-zinc-700 rounded-md mt-3">
                <div class="email text-sm text-blue-500 ">@<%= user.username %></div>
                <div class="content tracking-tighter text-base text-zinc-500"><%=post.content%></div>
                <div class="flex mt-1 gap-3">
                    <p class="text-blue-500 cursor-pointer">Like</p>
                    <p class="text-zinc-600 cursor-pointer">Edit</p>
                </div>
            </div>
        <%   }) %>
           
        </div>
    </div>
</body>

</html>